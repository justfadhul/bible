{
  "name": "spin-catalog",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A two-person daily Bible reading wheel. Spin, read the passage, discuss.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate": "node scripts/validate-catalog.mjs",
    "simulate": "node scripts/simulate-spins.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run validate && npm run test && npm run simulate && npm run build"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@vitejs/plugin-react": "^6.0.5",
    "tailwindcss": "^4.3.3",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
